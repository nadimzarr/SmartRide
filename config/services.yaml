# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    google_recaptcha_site_key: '%env(GOOGLE_RECAPTCHA_SITE_KEY)%'

services:
    # Default configuration for services in this file
    _defaults:
        autowire: true      # Inject dependencies automatically
        autoconfigure: true # Automatically register commands, subscribers, etc.
        public: false       # Default public value
    App\Service\InfoBipService:
        arguments:
            $apiKey: '%env(INFOBIP_API_KEY)%'
            $baseUrl: '%env(INFOBIP_BASE_URL)%'
            $senderId: '%env(INFOBIP_SENDER_ID)%'
            $logger: '@logger'
    # Ton service BadWordsFilter
    Vendor\BadWordsBundle\Service\BadWordsFilter:
        public: true
    App\Validator\BadWordsValidator:
        class: App\Validator\BadWordsValidator
        tags: ['validator.constraint_validator']
        autowire: true    
    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'
    App\Service\RecaptchaService:
        arguments:
            $recaptchaSecretKey: '%env(RECAPTCHA_SECRET_KEY)%'
    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
